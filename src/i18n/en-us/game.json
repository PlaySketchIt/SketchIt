{
    "$comment": "Extends en/game.json (en-GB is default en dialect, US overrides only what's needed)",

    "aria label": {
        "select custom color using picker": "Select custom color using picker",

        "select color": "Select color: {{color}}",
        "select color current": "Select color: {{color}} (current)"
    },
    "tooltip": {
        "click to select custom color": "Click to select custom color",
        "click to select color": "Click to select color:\n{{color}}"
    },

    "color": {
        "dark gray": "dark gray",
        "light gray": "light gray"
    }
}